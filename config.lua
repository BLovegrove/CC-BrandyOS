local cfg = {
    version = "0.0.1",
    remote_paths = {
        base = "https://raw.github.com/blovegrove/cc-BrandyOS/main/",
        startup = "startup.lua",
        config = "config.cfg"
    }
}

return cfg
